spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 1023
    url: jdbc:mysql://${DB_URL:tx.corele.top:3306/j-tools}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
    username: rooot
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
#    hibernate:
server:
  port: 8081
#      ddl-auto: create